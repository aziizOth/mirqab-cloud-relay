apiVersion: v1
kind: ConfigMap
metadata:
  name: relay-config
  namespace: mirqab-relay
data:
  environment: "dev"
  command_center_url: "http://command-center:8000"
  storage_backend: "local"  # local, s3, gcs, azure, minio
  # Override these in overlays
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: relay-service
  namespace: mirqab-relay
  labels:
    app.kubernetes.io/name: mirqab-relay
